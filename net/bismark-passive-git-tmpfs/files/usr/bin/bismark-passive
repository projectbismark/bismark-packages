#!/bin/sh

while [ 1 ]; do
    /tmp/usr/bin/bismark-passive.bin "$@" &
    trap "kill $!; rm -f $2; exit" INT TERM
    wait $!
    trap - INT TERM
    sleep 10
    echo "Restarting Bismark Passive"
done
